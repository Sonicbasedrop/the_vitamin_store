from django.apps import AppConfig


class ContactConfig(AppConfig):
    """
    A class for the contact configuration
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'contact'
